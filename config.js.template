var config = {}

config.idmURL = (process.env.IDM_URL || '');
config.client_id = (process.env.CLIENT_ID || '');
config.client_secret = (process.env.CLIENT_SECRET || '');
config.callbackURL = (process.env.CALLBACK_URL || '');

// Depending on Grant Type:
// Authorization Code Grant: code
// Implicit Grant: token
config.response_type = (process.env.RESPONSE_TYPE || 'code');

module.exports = config;
